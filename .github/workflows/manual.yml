name: Release|New/Reopened PR

on:
  pull_request:
    types: [labeled]

jobs:
  chat:
     runs-on: ubuntu-latest
     steps:
       - uses: geored/indy-action@v1
         with:
           url: ${{ secrets.WEBHOOK_URLS }}
