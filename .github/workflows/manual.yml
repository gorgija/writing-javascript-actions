name: Release|New/Reopened PR

on:
  pull_request:
    types: [labeled]

jobs:
  gchat:
     runs-on: ubuntu-latest
     steps:
       - uses: geored/indy-action
         with:
           url: ${{ secrets.WEBHOOK_URLS }}
