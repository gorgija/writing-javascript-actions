name: Release|New/Reopened PR

on:
  release:
    types: [created]
  pull_request:
    types: [labeled]

jobs:
  gchat:
     runs-on: ubuntu-latest
     steps:
       - uses: geored/indy-action@v6
         with:
           gchat-webhooks: ${{ secrets.WEBHOOK_URLS }}
